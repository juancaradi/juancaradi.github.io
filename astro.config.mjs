import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Tu dirección web final
  site: 'https://camilomath.xyz',
  
  // Como es tu repositorio principal, la base es la raíz '/'
  base: '/',
});