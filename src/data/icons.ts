// src/data/icons.ts

/**
 * 🚧 LIBRERÍA DE ICONOS (EN CONSTRUCCIÓN)
 * * Aquí irán los SVGs oficiales cuando decidas descargarlos manualmente.
 * Por ahora, este archivo es solo un placeholder para evitar errores de importación.
 */

export const icons: Record<string, string> = {
  // Ejemplo de cómo se verá en el futuro:
  // python: `<path d="..." />`,
  // r: `<path d="..." />`,
  
  default: `<circle cx="12" cy="12" r="8"/>`
};

/**
 * Función helper.
 * Por ahora devuelve vacío para no romper nada si se llama accidentalmente.
 */
export const getIconPath = (tool: string) => {
  return ""; // Retorna vacío por ahora.
};